// app.js - Main application entry point

// This file can be used for any global initialization or setup
// if needed in the future. For this simple application,
// the main logic is handled in ui.js, pdfHandler.js, and chat.js.

console.log('PDF Chat application started.');

// Example of calling an initial UI setup function (already done in ui.js)
// clearChat();
// setInputState(true);
// hidePdfInfo();